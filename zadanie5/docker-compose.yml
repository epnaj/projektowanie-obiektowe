services:
  server:
    build: ./server
    ports:
      - "8000:8000"
  client:
    build: ./client
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://server:8000
    depends_on:
      - server
