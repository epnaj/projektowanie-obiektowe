#extend("base"):
    #export("title"): Products #endexport
    #export("body"):
        <p><a class="button" href="/web/products/create">New product</a></p>
        #if(count(products) == 0):
            <p>Noo products available</p>
        #else:
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Price</th>
                        <th>Description</th>
                        <th>Quantity</th>
                        <th>Category</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    #for(product in products):
                        <tr>
                            <td>#(product.name)</td>
                            <td>#(product.price)</td>
                            <td>#(product.description)</td>
                            <td>#(product.quantity)</td>
                            <td>#if(product.category):#(product.category.name)#else:-#endif</td>
                            <td>
                                <a class="button" href="/web/products/#(product.id)">View</a>
                                <a class="button" href="/web/products/#(product.id)/edit">Edit</a>
                                <form class="inline" method="post" action="/web/products/#(product.id)/delete">
                                    <button type="submit">Delete</button>
                                </form>
                            </td>
                        </tr>
                    #endfor
                </tbody>
            </table>
        #endif
    #endexport
#endextend
